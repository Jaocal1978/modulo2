function potencia(base, exponente) 
{
	return Math.pow(base, exponente);
}

function sumar(x, y)
{
	return x + y;
}

function multiplicar(x, y)
{
	return x * y;
}

const MASA_TIERRA = 1000000;

export {potencia, sumar, multiplicar, MASA_TIERRA}